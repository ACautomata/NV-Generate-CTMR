"""Dataio adapters: augmentation and list-assembly. Pure domain math stays in ctmr.domain -- since #269 (ADR-0019 §3) morphology lives in ``ctmr.domain.morphology`` and the tumor-removal chain in ``ctmr.domain.generation.tumor_removal``; the augmentation re-export alias retired with the mask-family migration (#273)."""
