# P1 候选训练:DM 全参微调(rflow-mr-brain v1 续训)

- **落盘日**:2026-08-29(第一批统一实验记录,父任务 #186)
- **实际运行窗口**:2026-08-22 ~ 2026-08-23(训练与 dev 侧车,UTC);下游 holdout 生成与 L1 特征提取至 2026-08-24
- **状态**:run `p1-20260822T131947Z` 已 frozen;候选 epoch_20 当选;L2 终验 FAIL,L1 正式验收未跑,L3 评审未见
- **产物基目录**( sugon 集群,下文以 `$P1` 指代):`/root/private_data/brats2023_rflow_p1/`

## 目的与关联 issue

在 BraTS2023 脑肿瘤数据上,以 `rflow-mr-brain v1` 为底座做 DM 全参续训(VAE 冻结),交付 P1 image-only 带肿瘤候选:学习 BraTS 肿瘤外观与 T1c 序列,以模态标签为条件;候选经开发集轻验收与预录早停规则选择,并产出可供 L1/L2/L3 消费的证据。

- 关联:#51(Spec,BraTS2023 RF 分阶段微调配方)→ **#57**(P1 带肿瘤候选训练,CLOSED)→ #58(三层验收);父任务 #186(读数收编)
- 下游读数详录:#188(P1 L2 终验 FAIL)、#189(P1 L3 盲评包);本报告只记训练本体与其证据链状态

## 运行配置与 run_id

| 项 | 值 | 出处 |
| --- | --- | --- |
| run_id | `p1-20260822T131947Z`(schema brats-phase-run/1,status=frozen,frozen_utc 2026-08-22T22:46:48Z) | `$P1/records/runs/p1-20260822T131947Z/run.json` |
| 底座 checkpoint | `diff_unet_3d_rflow-mr-brain_v1.pt`(sha256 `90c4a015…c73b36`) | run.json `base_ckpt` |
| 训练脚本 | `/root/nv-phase-57/scripts/brats_p1_finetune.py` | `$P1/ckpt/train_provenance.json` |
| 超参 | lr **2e-06**、batch_size 1、n_epochs 上限 100、cache_rate 0 | `/root/nv-phase-57/configs/config_brats_p1_train.json`(`diffusion_unet_train`);`$P1/ckpt/train_provenance.json` `hyperparameters` 一致 |
| 硬件/精度 | world_size **7**(DCU)、amp bf16、torch 2.9.0 | `$P1/ckpt/train_provenance.json` |
| VAE | 冻结,复用 `autoencoder_v1.pt`(仅 encode/decode) | run.json `platform.trained_autoencoder_path` |
| 数据 | BraTS train list 7404 条 + MR-RATE replay list 7404 条,**1:1 回放混合**(token 34、augment_modality_label=0.1,#57 AC) | run.json `data_lists`(`p1_image_only.json` / `p1_mrrate_replay.json`);`$P1/lists/p1_mrrate_replay.json` 实测 7404 条 |
| dev list | 1060 条(dev 侧车评估自其中取 64 例/次) | `p1_image_only_dev.json`(run.json data_lists);`$P1/dev_eval/epoch_20/samples.json` 实测 64 条 |
| dev 侧车生成口径 | RFlowScheduler、cfg_guidance_scale 10、30 步 | config_brats_p1_train.json `diffusion_unet_inference`;`$P1/holdout_generated/EXECUTION_NOTES.md` |

时间线(UTC):run 记录创建 08-22T13:19Z → 起训 08-22T16:20Z(`$P1/logs/train_20260822T162028Z.log`)→ 训练结束 08-22T22:00Z(日志尾部 NCCL destroy;ckpt 目录 epoch_1~epoch_36 mtime 08-23 00:31~05:58 CST)→ selection 记录 08-22T22:46:43Z → run frozen 08-22T22:46:48Z。dev 侧车评估 e5~e35 于 08-22T18:00~22:00Z 逐点产出(selection.json `trend[].eval_utc`)。

## 关键读数与产物路径

### 候选选择与早停

- **选中候选:epoch_20,mean dev FID 3.3980**——出处:`$P1/selection.json`(`"epoch": 20, "mean_fid": 3.3979697571412877`;注意该文件在基目录根,不在 dev_eval/ 下);`$P1/logs/select.log`(「selection → … (epoch 20, mean_fid 3.3980)」);run.json `selection.checkpoint`(epoch 20,`$P1/ckpt/epoch_20.pt`,sha256 `9377f8ba…12946b`)
- 选择规则:`argmin mean dev FID over eval points (pre-recorded)` + `no new best for 3 evals`——出处:selection.json `rule`;run.json `selection.rule`
- **早停原文**(预录规则在 e35 评估后触发):`$P1/ckpt/.early_stop` 逐字——

  ```json
  {"reason": "no new best for 3 evals (best 3.3980)", "epoch": 35}
  ```

- 实际训练 36 epochs(非 100 上限):`$P1/ckpt/latest.json`(「`{"epoch": 36, "checkpoint": "…/epoch_36.pt"}`」);ckpt 目录内 epoch_1~epoch_36 共 36 份
- **dev FID 趋势**(selection.json `trend[].m`,各点标注 eval_utc):

  | epoch | 5 | 10 | 15 | **20(选中)** | 25 | 30 | 35 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | mean dev FID | 3.8536 | 3.9258 | 3.5943 | **3.3980** | 3.6323 | 3.8067 | 3.5934 |

  e20 分序列读数(`$P1/dev_eval/epoch_20/trend.json`):t1n 3.370 / t1c 3.323 / t2w 3.220 / t2f 3.678

### holdout 生成(候选证据)

- **530 例 × 4 模态 = 2120 卷,生成完整**——例数出处:`$P1/holdout_generated/samples.json`(530 条);卷数出处:`generated/` 下实测 `.nii.gz` 共 2120(GLI 1000 / MEN 800 / METS 192 / PED 80 / SSA 48,按肿瘤挑战目录);口径出处:`$P1/holdout_generated/EXECUTION_NOTES.md`(「总 530 例 × 4 模态 = 2120 卷」;530 = GLI 250 / MEN 200 / METS 48 / PED 20 / SSA 12)
- 模态 = 每例 4 序列(t1n/t1c/t2w/t2f),文件名样例 `BraTS-GLI-00016-000_t1c_seed1177018642.nii.gz`;8 卡分片(`shard_0..7`,`logs/shard_*.log`),seed=sha256(case|modality),int16 ×1000(EXECUTION_NOTES.md 口径决策)

### L1/L2/L3 状态

- **L1:特征已提取,正式验收未跑**。特征:`$P1/holdout_l1_features/challenge_{GLI,MEN,METS,PED,SSA}/`(每挑战 features.json + per-plane npy,共 1091 个文件);缺口:`$P1/l1_acceptance/evaluate.log` 为 **0 字节**——正式量化验收从未产出
- **L2:终验 FAIL**——`$P1/l2_acceptance/evaluate_v1/l2_final_acceptance_p1.json`(`overall_verdict: "fail"`);详细读数见 #188 报告
- **L3:盲评包已建,评审结论未见**——`$P1/l3_acceptance/{package.json,blind_map.json,catalog.json}`;见 #189 报告

## 结论与后续动作

**结论**(草拟待复核):

- P1 候选训练本体完成且可复现:全参续训 36 epochs 后按预录规则早停,epoch_20 以 best dev FID 3.3980 当选,run 已 frozen(run.json status=frozen),holdout 2120 卷证据完整。
- 三层验收链未走完:**L2 终验 FAIL**(五挑战 TOST 未过,#188)、**L1 正式验收未跑**(evaluate.log 0 字节)、**L3 评审结论未见**——run 尚未 conclude(EXECUTION_NOTES.md「后续」清单原话:「L1 → L2 → L3 → attach l1/l2 → (L3 评审后 attach l3 + conclude + dm_source)」)。
- 与 issue 状态的偏差:#57 已 CLOSED(其 AC 以开发集轻验收+预录早停+阶段契约为准,均满足);但候选级 conclude 依赖的三层验收中 L2 为 FAIL、L1 未落盘——该偏差由父任务 #186 的偏差清单统一记录,不在此翻案。

**后续动作**(草拟待复核):

1. 补跑 P1 L1 正式验收,填补 `$P1/l1_acceptance/evaluate.log` 空缺,报告落盘本目录。现役入口 `ctmr accept quantitative evaluate`;当时的服务器侧执行命令与参数(--bootstrap-resamples 1000 --seed 20260821,系已退役 scripts 层历史命令)录于 `$P1/holdout_generated/EXECUTION_NOTES.md`,重跑时旗标以 `ctmr accept quantitative evaluate --help` 为准。
2. L2 FAIL 整改与重验(#58,详见 #188 报告)。
3. 认领 P1 L3 盲评(200 格,#56/#58),评审后 conclude + dm_source 注册。
4. P2 ControlNet 已自 epoch_20 起训(#59),其读数由 #190 报告承载。
5. (记录义务)后续若 P1 相关实验推进,新增后日报告,不改写本文件。
