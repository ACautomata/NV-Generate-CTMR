# 序列③ T2:编码链 resize 目标修复(RAS spatial_shape)与全量 embedding 重编码

**总体结论**:vendored 编码链 `diff_model_create_training_data` 的 resize 目标决策改从 **RAS 重排后 spatial_shape** 读取(替代 NIfTI header 原始轴序 dim),3 枚单测钉板(非对称体积 + SLA affine → (128,256,256) 而非旧路径的 (256,256,128));sugon 全量重编码(P1 主 7404 + replay 7404 + dev 1060 = 15868 条驱动编码,P2/P3 臂 8464 引用对账+守卫)至新树 `embeddings_ras/`,manifest 对账与逐条形状守卫全绿,旧树(legacy/cliptrue)零触碰。冒烟双对照臂读数与作业 C 双链锚同档(直通 0.0076≈0.0062、工件 0.0759≈0.0823)——修复落地、环境无漂移。variant=diagnostic,冻结 VAE 只读,评估链与判定线零改动。 | **记录落盘日**: 2026-09-03

## 1. 目的与关联 issue

2026-09-03 方向审计(#310)实测 replay(MR-RATE)臂 embedding 形状轴序错乱 ~65%(cliptrue 树 (32,64,64,4)×2953、(64,64,64,4)×1524、(64,32,64,4)×315 vs canonical (64,64,32,4))。根因取证:编码链 resize 目标 `round_number(header dim[1:4])` 用 NIfTI **存储轴序**,而管线里 `Orientationd(axcodes="RAS")` 对换轴方向体积会置换轴序——resize 目标与 RAS 后实际形状错配,轴序错乱工件被全卷积 DM 静默吃进训练。BraTS 臂侥幸免疫的两层事实:①方向分布 LPS/RAS 全是 flip-only(不换轴);②即便换轴,x/y 对称 (240,240) round 后仍不可分——免疫是数据巧合,不是代码正确(历史如实登记)。

本票 = 修复 + 全量重编码:新树为 T5(P1 重训)/T6/T7(P2/P3)提供干净方向世界的 embedding 底座。

- 关联 issue:#312(本票)、#310(序列③父票)、#251(T4 先例:clip 编码与重编码)、#313(T3:loader 守卫,消费本票新树)、#315(T5:P1 重训)
- 判定状态关联:variant=diagnostic,不产生任何验收判定

## 2. 代码改动(本地)

- `ctmr/infrastructure/maisi_engine/create_training_data.py`:新增 `new_dim_from_ras_shape(image)`(读 RAS 重排后 `shape[1:4]` → round_number),`diff_model_create_training_data` 内 new_dim 决策改调之。vendored 记录偏差沿 instance_definition 先例——模块头注释登记 #312 依据与 BraTS 免疫历史
- 单测 `tests/infrastructure/maisi_engine/test_engine_smoke.py`(3 枚):
  - `test_new_dim_from_ras_shape_reads_reoriented_axes`:非对称 (240,240,155) + SLA affine(RAS 后 (155,240,240))→ (128,256,256) ≠ 旧路径 (256,256,128)
  - `test_new_dim_from_ras_shape_agrees_with_header_order_for_flip_only_directions`:LPS 对照,免疫情形如实记录
  - `test_ras_shape_derivation_matches_monai_orientation`:nib as_closest_canonical ≡ MONAI Orientationd 形状(守卫与编码链双源钉)
- 重编码作业模块 `ctmr/application/generation/modality_label/reencode_ras.py`(沿 T4 reencode 同族,16 枚单测):`EmbeddingShapeGuard`(逐条从 raw 推导期望 latent 形状 = RAS 重排 → 128 基取整 → ÷4 → channels-last;非规范形状/unreadable → 违例,作业失败先落盘报告;取整语义本地复述并逐点钉 vendored round_number——modality_label 家族零 infrastructure import 分层约束)、`EmbCohortList`(P2/P3 list 的 emb 引用解析,`embeddings/` 前缀断言)、`SidecarMultiSource`(多源 fallback 拷贝+校验)、`ReencodeRasReport`(schema `embedding-reencode-ras/1`,issue 312,父 310)
- 作业脚本 `deploy/jobs/run_embedding_reencode_t2.sh`:双 source、前置校验(含修复存在性 grep)、**dev 臂 raw 幂等补链**(dev 1060 的 raw 只在 `raw_brats2023_official`,raw_relinked 镜像树缺——补符号链接使编码链与守卫同 data_base_dir 解析)、SHARDS=4 分片(每片单卡 CUDA_VISIBLE_DEVICES 隔离)+ setsid nohup + 收尾进程(守卫+manifest+对账+抽检+报告)
- 全量 pytest 1045 passed / 12 skipped(gpu 预期);ruff check+format 全绿;分层守卫与 #272 family AC 全绿
- 代码合入:PR #319

## 3. sugon 执行环境(2026-09-03 实例 `crdnotebook-2094475601641066498-wang9691-43400`)

- 与 T4 末期实例同快照延续:部署树 `/root/nv-phase-t4/`(instance_definition md5 `5cd8c3c7…` = T4 登记 clip=True 版)与 python 环境(torch-dcu 2.9.0/monai 1.6.0/nibabel 5.4.2/numpy 1.26.4,与 T4 登记逐项一致)均在;src 树整包重传(create_training_data md5 `2463b8fe…`、reencode_ras md5 初版 `4fc337f0…`/修补后 `cec5cb47…` 与本地复验一致)
- **部署树易失盘语义(如实登记)**:本作业沿 T4 先例用系统盘部署树 `/root/nv-phase-t4/`(T7 已改进为持久盘 `/root/private_data/ctmr/deploy_t7`,本票未跟随——作业已拉起未动)。实例重分配后需重传 src 续跑;编码链 skip-if-exists 兜底已编码条目零浪费(T4 先例两次验证)
- 数据树(持久盘):lists 四件(p1_image_only 7404 / p1_image_only_dev 1060 / p2_mask_cond 8464 / p3_pairs 25392);replay list `runs/p1/lists/p1_mrrate_replay.json` 7404(其中 sag 3812;sidecar 经 T7 前置 rebuild_replay_sidecars.py 重建进旧根,本作业多源拷贝消费);embedding 旧树 legacy 8465 / cliptrue 14808(均不动);raw_relinked 覆盖训练 7404 + replay,dev raw 由本作业补链自 `raw_brats2023_official`(1060 全在);VAE `autoencoder_v1.pt`(冻结只读);磁盘余量 537G;4×DCU 空闲
- **口径注记**:issue 票面「P2/P3 臂 8465」= 旧 embedding 树 `embeddings/` 的文件数;P2 list 实为 8464 条(= P1 主 7404 + dev 1060 的 embedding 引用全集),P3 25392 对引用同一集合。本作业按 list 实测口径执行:raw 臂编码 15868 = 7404 + 7404 + 1060,P2/P3 臂以 8464 唯一引用路径对账+守卫

## 4. 关键读数与产物路径

产物(sugon 工件区,不入 git):冒烟 `runs/p1/reencode_t2_smoke/`,全量 `runs/p1/reencode_t2/`;新树 `data/phase/embeddings_ras/`。

### 冒烟(5 例:t1c/t1n 各一 + replay sag ×2 + dev ×1,全链路)

编码 5/5;manifest 5 entries、missing 0、形状守卫违例 0。RAS 修复后的新树形状:BraTS (64,64,32,4);sag 条目按各自 RAS 后几何落 (32,64,64,4)/(64,64,64,4)(D2YKHZ32YF old dim (160,256,256)→new dim (128,256,256);5OILKX2INZ (192,256,256)→(256,256,256))。

**双对照臂条件 MAE(锚为作业 C 第五轮读数,参照值非判定线)**:

| 臂 | 冒烟读数 | 作业 C 锚 | 判读 |
|---|---:|---:|---|
| 直通臂 [0,1] 域内 | **0.0076** | 0.0062 | 同档——VAE/归一化/resize 环境与作业 C 一致 |
| 工件臂 [0,1] 域内 | **0.0759** | 0.0823(旧工件同链自评) | 同档——新工件与旧工件同构,修复只影响 resize 目标决策 |

### 全量重编码(15868 条,进行中)

- 执行:4 卡分片(`SHARDS=4`,每片 3967 条)+ 收尾单卡(`--skip-encode`:sidecar 多源拷贝 + manifest 对账 + 逐条形状守卫 + 抽检 200 + 报告);setsid nohup 脱离会话;编码链跳过已存在文件,实例易失可续跑(T4 先例)
- 终读数:【全量完成后补】manifest 条目数/missing/违例、抽检聚合、新树体积

## 5. 结论与后续动作

- [x] dim 修复合入(PR #319),3 枚单测钉板(非对称体积 + 非 RAS affine 断言)
- [x] 冒烟全绿(形状守卫/对账/环境同档)——修复落地
- [ ] 全量重编码完成 + manifest 对账全绿 + 守卫全绿(作业进行中,完成后补 §4 终读数)
- [x] 旧树(legacy/cliptrue)零触碰;新树 `embeddings_ras/` 落盘
- [ ] 实验记录落盘(本文件;终读数补齐后闭票)

后续动作:

- T3(#313)训练 loader 双守卫(Orientation RAS + 非规范形状拒绝)消费新树;T5(#315)P1 重训 env json 改指 `embeddings_ras/`(T4 先例:改 embedding_base_dir 即可)
- T6/T7(P2/P3 重训)list 的 `embeddings/` 前缀需映射到新树根(或重生成 list)——本票未动四个 list
- sidecar spacing 值沿 T4 语义拷贝自旧树(raw pixdim,轴序语义待 T3 loader 定夺——如实登记)
- 旧树与旧 raw 保留为回退锚,暂不清理
- KNOWN DEBT:抽检诊断种子沿用 T4 块(base+400..407),待 challenge_registry follow-up 一并登记
