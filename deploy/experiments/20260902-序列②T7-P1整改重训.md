# 20260902 序列②T7——P1 整改重训 run（单臂三改动）

## 目的与关联 issue

issue #254（父 #247 序列②整改重训）。以基底 run `p1-20260822T131947Z` 同款配方执行整改
重训，改动恰三项：①编码 clip=True（T4 重编码树 embeddings_cliptrue/，replay 经 raw 重获取
后同根重编码——负责人决策扩展施用面）；②token 34 冻结（T3，frozen_modality_tokens=[34]）；
③写出 affine（T2，代码面已合入）。epoch/lr/1:1 replay/CFG=10/采样步数一概不动；不做并行
消融。重训定位为**假设检验**：「协议修正包足以恢复空间保真」——判定面在 #247 终验（L2
全量），本记录的 dev FID 仅是选择仪器读数。

## 运行配置

- **run 根**：`/root/private_data/ctmr/runs/p1_t7/`（ckpt / ckpt/dev_eval / logs / selection.json /
  recipe_diff_checklist.json）；部署树 `/root/private_data/ctmr/deploy_t7/`（src=main@53bf69b 后同步）。
- **作业**：`deploy/jobs/run_p1_retrain_t7.sh`（--dry-run 先行核对：协议改动恰 3、登记偏差恰 1、
  不动 17；preflight 含底座 sha256/VAE md5/config T3 终态钉值/network config 对基底 run.json
  机读对账/基底超参逐键断言/P3 e39 sha 观测）。正式拉起 2026-09-01T14:15Z（前两次拉起分别因
  集群 src 未同步旧形态、旧 worker 残留占 rendezvous 29500 端口而失败，清场后重开——详见
  20260901 前置记录与 launcher commit e533782）。
- **provenance**（ckpt/train_provenance.json）：lr 2e-6、batch_size 1、n_epochs 100、cache_rate 0、
  frozen_modality_tokens [34]、amp bf16、torch 2.9.0（DCU）、底座 diff_unet_3d_rflow-mr-brain_v1.pt。
- **拓扑（偏差 A，#254 会话决议）**：world_size 4（基底 7）——4×DCU 实例全卡训练，有效 batch
  与 PolynomialLR total_steps 随拓扑变化；登记偏差 B：replay 编码同 clip=True。
- **dev 监控形态**：trainer 内嵌周期验证（#278/#279 现役形态，ADR-0019 §5），`--val-every 5`
  对齐基底评估网格；16 例×4 模态全卡分片，cfg=10/30 步同钉值；早停规则 ADR-0005 钉值
  （patience 3 / min_epoch 30 / max=100）经 `<ckpt_dir>/.early_stop` 内嵌生效。reference bank
  用同仪器重提产物（`p1/dev_eval/reference_reinstr/`，见前置记录 §4），T7 与基底 trend 全程
  同栈同 bank 可比。
- **时长**：2026-09-01T14:15Z 拉起 → 09-02T05:18Z 早停写盘 → 05:23Z 进程收尾，约 15.1h；
  ~19-21 min/epoch（含验证摊销）。

## 关键读数

### dev FID trend（同仪器口径，8 点；括号为基底同仪器重算值）

| epoch | 5 | 10 | 15 | 20 | 25 | 30 | 35 | 40 |
|---|---|---|---|---|---|---|---|---|
| **T7 m** | 4.5212 | 4.2609 | 4.0253 | 4.0331 | **3.6690** | 3.8296 | 3.8124 | 3.8801 |
| 基底 m | 3.8291 | 3.9071 | 3.5889 | 3.4289 | 3.6289 | 3.8317 | 3.5644 | — |

- **候选（select 合同：argmin mean dev FID）**：**epoch 25，m=3.6690**；
  `selection.json` 落盘（rule=pre-recorded argmin）。
- **早停**：`{"reason": "no new best for 3 evals (best 3.6690)", "epoch": 40}`——e25 best 后
  e30/e35/e40 连续无新 best，patience 3/3 满于 e40 边界触发，零 kill 干预。
- **候选 checkpoint md5**：`epoch_25.pt` = `0dac81864b68ab35f16dde93039db643`。
- **loss 曲线要点**：L1 loss 全程 0.76–1.06 波动（bs1 单步噪声主导，与基底训练同特征），
  无发散/平台异常；首 epoch ~0.99、末 epoch 40 ~0.78–1.01。

### 初步判读（选择仪器面，非验收判定）

- T7 trend 形状与基底同族：前段高位爬坡、中段 best、尾段平台。T7 的 best 出现更晚
  （e25 vs 基底 e20）且仍处下降通道时被 patience 截停（e25→e30 的 +0.16 回升贡献了 3 连）。
- **T7 dev best 3.6690 vs 基底同仪器 best 3.4289：高 0.240（+7.0%）**——dev FID 面 T7 未
  追平基底水平。三项协议改动的净效应在该仪器上读数为负向或中性（无法从此读数分解单项），
  但**假设检验的判定面是 #247 终验（L2 全量空间保真读数）**，dev FID 不是验收线；空间保真
  是否恢复以 T6 监控链与终验为准。
- 同仪器口径的成立依赖：同 RadImageNet 权重（md5 5d9c98ab…）+ 同预处理栈 + 同 bank
  （reference_reinstr）。基线历史数字（3.3980 旧栈）不与本表直接比较。

## 结论与后续动作

1. 重训 run 完成：候选 `epoch_25.pt`（md5 `0dac8186…`）已登记，selection.json/核对表/
   dev_trend.jsonl 全部落盘于 `/root/private_data/ctmr/runs/p1_t7/`。AC1/AC2 达成；
   AC4 冻结面零改动（P3 e39 sha 观测 b005a1c76389…，冻结仪器/包络/判定线未触碰）。
2. 假设检验进入判定段：候选送 #247 终验链（T6 dev 选择点 ET/WT 监控 → L2 全量 14 量族）
   ——「协议修正包是否恢复空间保真」以终验读数裁决，本记录不预判。
3. dev FID 未追平基底 best 的读数连同 8 点 trend 如实上报；若终验 FAIL，#247 预埋后手
   清单（z-pad 留观触发条件等）按既定次序启用。
4. 产物路径速查：训练日志 `logs/train_20260901T141537Z.log`；配方核对表
   `recipe_diff_checklist.json`（协议改动 3/偏差 1/不动 17）；逐点 ledger
   `ckpt/dev_eval/dev_trend.jsonl`；早停件 `ckpt/.early_stop`。
