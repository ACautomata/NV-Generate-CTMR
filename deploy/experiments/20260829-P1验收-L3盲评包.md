# P1 L3 盲评包状态(包就绪、评审待认领)

> 记录落盘日:2026-08-29。实际运行窗口:盲评包构建于 2026-08-24(依产物 mtime),素材生成于 P1 候选训练 run(`p1-20260822T131947Z`,2026-08-22 起);采集日 2026-08-29。关联 issue:#186(父)、#58、#56。

## 一、目的与关联 issue

记录 P1 L3 盲评包的建设状态。本记录是「包就绪、评审待认领」的**状态记录**:三个 json(blind_map / catalog / package)已建成且经完整性核对,但**评审结论未见**——评审阶段尚未开始,本记录不给出、也不代表任何评审结论。

关联 issue:

- **#56(L3 盲评包与判定,CLOSED)**:定义盲评包契约——按目标模态×子挑战抽取 5 真实 + 5 合成、共 200 条目,抽样与盲化可复核;判定走 visual-Turing balanced accuracy CI、四维 Likert 单侧 CI 与 Fleiss' κ。本包按其契约建成,但评审与判定未执行。
- **#58(P1 完整验收与冻结,CLOSED)**:要求 L1∧L2∧L3 非补偿终验。L2 已 FAIL、L3 评审结论未见(见「四、结论」),其 CLOSED 状态与本记录存在偏差。

## 二、运行配置与 run_id

| 项 | 值 | 出处 |
|---|---|---|
| run_id | `p1-20260822T131947Z` | package.json、blind_map.json 顶层字段 |
| phase | `P1` | 同上 |
| 采样种子 | `20260821` | package.json `sampling.seed` |
| per_cell | real 5 + synth 5 | package.json `sampling.per_cell` |
| total_entries | 200 | package.json `sampling.total_entries` |

素材来源(依 entries 内 path):合成图出自 P1 持出集生成产物 `/root/private_data/brats2023_rflow_p1/holdout_generated/generated/…`(文件名含 seed);真实图出自 BraTS2023 原始训练数据 `/root/private_data/brats2023_rflow_phase/raw/ASNR-MICCAI-BraTS2023/…`,catalog 内每条记录附 `sha256`。

## 三、关键读数与产物路径

产物目录:`/root/private_data/brats2023_rflow_p1/l3_acceptance/`(曙光实例,2026-08-29 SSH 只读采集)。

文件清单(共 3 个文件,目录内无其他产物):

| 文件 | schema | 大小(B) | mtime |
|---|---|---|---|
| `catalog.json` | `brats-l3-catalog/1` | 1,534,457 | 2026-08-24 00:20 |
| `package.json` | `brats-l3-package/1` | 54,948 | 2026-08-24 00:21 |
| `blind_map.json` | `brats-l3-blind-map/1` | 67,250 | 2026-08-24 00:21 |

包结构读数(以下计数均在 2026-08-29 采集时对产物文件现场核对):

- **20 格 × 10 条 = 200 条目**:5 子挑战(GLI / MEN / METS / PED / SSA)× 4 目标模态(t1n / t1c / t2f / t2w);每格 5 real + 5 synth(package.json `entries` 分组计数;blind_map.json 逐 (格, source) 计数恒为 5)。
- entry_id 形如 `L3-0001`,无重复;package(盲)与 blind_map(解盲)的 entry_id 集合完全一致。
- blind_map source 分布:synth 100 / real 100(blind_map.json `entries`)。
- catalog 抽样池共 4,240 条:real 2,120 + synth 2,120;按 challenge GLI 2,000 / MEN 1,600 / METS 384 / PED 160 / SSA 96;按模态各 1,060(catalog.json `records`)。
- 200 条 entry 的 `image_path` 全部存在(全量 `os.path.isfile` 核对,0 缺失)。
- 盲化机制:package.json 每条仅含 `entry_id / challenge / target_modality / image_path`,不含 case 与 source;解盲信息(`case`、`source`)只出现在 blind_map.json——评审人只持 package 即可盲评。

**L3 状态:评审结论未见。** 产物目录内无任何评审结果文件;截至采集日,visual-Turing / Likert / Fleiss' κ 判定均未执行。**本记录不得读作「P1 L3 已验收」。**

## 四、结论与后续动作

**结论(草拟待复核):** 盲评包按 #56 契约建成且可复核——200 条目、20 格、per_cell=5、种子与 sha256 齐备、图像文件零缺失、盲化解盲分离干净。但评审阶段未启动:**#56 的 CLOSED 状态与「评审结论未见」之间存在偏差**;#58 的 L3 分支同样悬空,且其 L2 已 FAIL。P1 整体验收不能凭本包判过。

**后续动作(草拟待复核):**

1. **L3 评审认领**:招募 ≥2 位独立评审,持 package.json(盲)完成 visual-Turing 与四维 Likert 评分;解盲统一走 blind_map.json。
2. **与 #56 判定流程衔接**:汇总 balanced accuracy CI(须完整位于 [0.40, 0.60])、四维 Likert 单侧 CI(下界 ≥ 4.0/5.0)与 Fleiss' κ;评审结论以**新增后续日期的报告**落盘,不改写本快照。
3. 结合 #58 L2 FAIL 的事实,评审结论仅作诊断输入;P1 整体结论依三层非补偿合取判定,不因 L3 单层结果翻案。
