# 20260830 服务器目录聚合与清理

## 目的与关联 issue

sugon 实例 `/root/private_data` 顶层散落 CTMR 数据目录、其他项目遗留物与临时垃圾，home（易失系统盘）堆积 4 代 `nv-phase-*` 旧源码与 3 个 core dump。本次：

1. 建立持久盘聚合根 **`/root/private_data/ctmr/`**，全部 CTMR 数据目录收拢其下；
2. 仓库内所有引用（jobs 配方 / src 默认值 / tests 断言）同步改写（commit `a7142cc`）；
3. 删除非 CTMR 项目遗留物与过期 smoke/中间产物（用户逐项确认）；
4. 源码统一部署到 `/root/ctmr`（本地 main rsync，取代 nv-phase-* 系列散落部署）。

## 运行配置

- 源码部署：本地 main rsync → `/root/ctmr`（系统盘，实例重置后需重传）。
- 数据聚合：同盘 `mv`（rename，零复制），两轮完成——先浅聚合收进 `ctmr/`，再语义化分组。最终路径映射：

| 旧路径（/root/private_data/…） | 新路径（/root/private_data/ctmr/…） |
|---|---|
| brats2023_rflow_phase | data/phase |
| brats2023_nnunet | data/nnunet_raw |
| nnUNet_preprocessed | data/nnunet_preprocessed |
| brats2023_rflow_p1 / p2 / p3 | runs/p1 / p2 / p3 |
| nnUNet_results | instruments/nnunet_results |
| l2-synth-eval | eval/l2_synth |
| ENVIRONMENT_LOCK.md | ENVIRONMENT_LOCK.md（聚合根顶层） |

悬空引用一并归位（首次运行时产生）：`l2-instrument` → `instruments/l2_instrument`、`l2-instrument-calibration` → `instruments/l2_instrument_calibration`、`models` → `instruments/v1_models`、`raw` → `data/raw`。`PERSISTENT_ROOT=/root/private_data` 持久盘守卫常量语义不变，未动。

- 删除清单（已授权）：home——`core.23910/25775/26044`（共 11.3G）、`nv-phase-52/57/59/60`、`manifold`（源码）；private_data——`core.7302`（4.3G）、`weibu`、`manifold`（runtime）、`BraTS-2024-Complete*`、`datasets/`（与 `ctmr/brats2023_rflow_phase/raw` 重复的原始数据副本）、`nv-dcu-smoke`、`p1_replay`、`brats2023_rflow_phase_smoke`、`nnUNet_results_compile_smoke(_single)`、`nnUNet_results_ssa_bs16_ddp8`、`synapse-wizard`、`images_build`、`tblogs`、`job_example`、`__pycache__`、`rsyncd.*`、散落脚本（`nnunet_ddp_preflight.py`、`nnunet_plan_variant.py`、`run_nnunet.py`、`p3_data_check.py`——均已收编入 `src/ctmr/infrastructure/provisioning/` 或属一次性脚本）、`logs_*_502*.txt`。

## 关键读数与产物路径

- 仓库端：16 文件路径同步（9 jobs + 4 src + 3 tests + deploy/README 环境锁路径），全量 pytest **756 passed / 2 skipped**（gpu 标记），pre-commit（ruff / ruff-format / markdownlint）全绿。
- 服务器端：聚合后 `/root/private_data` 顶层仅剩 `ctmr/`、`SothisAI/`、`wheels/`（DCU 安装包缓存，保留）与平台 dotfiles。
- 插曲：15:49 一次作业 D 启动尝试因 P1 checkpoint 已迁走 `[FATAL] 前置文件不存在` 退出，遗留 `brats2023_rflow_p1/token_dilution/` 13K 残骸（pid 已死），于收尾清除；配方已指向新路径，重跑即可。

## 结论与后续动作

- 今后所有实验配方默认读 `/root/private_data/ctmr/...`，源码部署唯一入口为 `/root/ctmr`（rsync from repo main）。
- 实例重置后的恢复顺序：① bashrc 双 source 注入（sugon-bootstrap）② wheels 重装 DCU 栈 ③ rsync 源码到 `/root/ctmr`；数据在持久盘聚合根，不受影响。
- 历史实验记录（experiments/*.md）中的旧路径为当时事实，不作回改；旧路径可按下表机械映射到新路径。
