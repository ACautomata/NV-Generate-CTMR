# 序列②T6:dev 选择点 ET/WT 监控链路 + 现候选 dev 基线读数

**状态: 全链完成——采样 520/520 + 仪器 v2 推理 260/260 + 测量 + 报告;观察线黄旗(METS ET 检出率 + GLI/SSA 体积线)** | **记录落盘日**: 2026-09-01(读数回填 2026-09-03)| **实际执行**: 2026-09-01 起,sugon 集群

## 1. 目的与关联 issue

把候选选择点从 dev FID-only 盲区去盲(收编裁决 #5 采纳项):在 dev 选择面建立 ET/WT 监控链路——dev list 分层样本以候选 checkpoint 采样伪四模态体 → 冻结仪器只读路径 → 作业 B 口径 ET 甄别 + WT 添注读数 → 观察线黄旗判定;并对现 FAIL 候选(run `p1-20260822T131947Z`)产出「重训前」dev 基线,链路 shakedown 与 T8 疗效对照基线一箭双雕。

- 关联 issue: #253(本票,序列②T6)、父 #247(序列②整改重训)、#210(收编裁决)、#207(作业 B,读数口径来源)
- 判定状态关联: variant=diagnostic,选择面、非验收判定;P1 L2 判定不变(FAIL)
- 观察线(预记录,选择面): **METS ET 检出率 < 0.9 或任一挑战 vol_et_rel 中位 > 2 → 黄旗**

## 2. 运行配置与 run_id

- **代码**: 本地分支 `worktree-modular-nibbling-whistle` commits `d61de00`/`52454dd`/`a5622b7`(观察线纯函数 `observation_line.py`、监控报告臂 `dev_monitor.py`、采样臂 `dev_monitor_sampling.py`、种子槽位登记、`deploy/jobs/run_dev_monitor_etwt.sh` 配方),rsync 至 sugon 部署树 `/root/ctmr`;**续跑补充(2026-09-03,`1baae75`/`7d886a9` + WtMonitor undefined 修复)**:配方第二步b 仪器 spec 实况覆写(见 §5-6)、第四步 `nnUNet_extTrainer` 注入(见 §5-7)、报告臂 WT 轴非正 real 分母排除并计数(TDD 红→绿)
- **run_id**: `p1-20260822T131947Z`(T6 现候选基线;T8 重训候选换 `CKPT`+`RUN_ID` 重跑同链路)
- **checkpoint**: `/root/private_data/ctmr/runs/p1/ckpt/epoch_20.pt`(冻结候选,只读);VAE `/root/private_data/ctmr/models/autoencoder_v1.pt`(冻结,只读;经 `env_monitor.json` 绝对路径覆写件解析)
- **仪器版本(读数对照须知)**: 本次测量消费 **v2 仪器**(重训换树后权重;2026-09-01 仪器主本丢失,见 `20260901-仪器主本丢失与重训决策.md`)——读数与 T5 历史读数**不可直接比仪器版本**;校准重跑 + spec 冻结锚重钉 + 新 ADR 为 T8 前置,不在本票范围
- **采样协议**(钉定于 `dev_monitor_sampling.MONITOR_QUOTAS`,非 CLI 旋钮):
  - 分层配额 **GLI 50 / MEN 40 / METS 24 / PED 10 / SSA 6 = 130 例**(dev list 1060 条 = 265 例的分层样本;sha256(challenge/case) 序选择,确定性可复现;配额不足即失败,不静默缩样)
  - 每例 4 模态(t1n/t1c/t2w/t2f)= **520 采样体**;每 (case, modality) 一 seed(`sha256(case|modality)`,sidecar 惯例,P1 伪四模态独立种子义务)
  - 冻结采样配方:cfg=10、30 步、RFlowScheduler(候选 dev 证据同款);写出协议 #249 后世界(affine = 真实采样 spacing (0.94, 0.94, 1.36),单例 smoke 逐文件验证)
  - **population = dev,零 holdout 接触**:dev list 是唯一 population 输入;holdout 530 的任何工件不进入本链路
- **种子纪律**: WT 添注 bootstrap CI90 取新登记诊断槽位 `dev_monitor_wt_rel_diff = 600`(诊断基 900,000,000 + 挑战偏移 × 1000 + 槽位;T5 基线 #252 落地 400/500 两臂后让位重注册,先档 400 在首次取用前变更,零位流影响;无碰撞不变量由单测钉住);ET 轴沿用作业 B 槽位 200(经未改动的 `EtDiscrimination`);全程不触 GLOBAL_SEED=20260821 判定链
- **冻结仪器只读路径**: plan 与终验 `l2-final-acceptance-plan/1` 同构 → `final_acceptance predict`(逐挑战冻结 spec/TTA 冻结口径)+ `measurement_run assemble-execute`(gen 重采样 + RAS→LPS 翻转;real 原生直通)+ `measurement_run measure`(MEASUREMENT_FIELDS CSV)零改动原样复用;仪器、包络、判定线零改动
- **执行环境**: /usr/local/bin/python 3.11.9 + DTK 26.04(torch 2.9.0+das.opt1、MONAI 1.6.0、flash_attn 2.8.3 DCU 构建,wheels 缓存重装)+ nnunetv2 2.8.1(实例重置后按 deploy/README 恢复顺序补齐;einops/pytest/synapseclient 经代理补装)

## 3. 关键读数与产物路径

- 采样产物: `/root/private_data/ctmr/runs/p1/dev_monitor/samples/`(**520/520 完成**,2026-09-01;受控存储不入 git)
  - 核验:日志 `[sample]` 恰 520 条、零错误;逐挑战体数 GLI 200 / MEN 160 / METS 96 / PED 40 / SSA 24(= 配额 ×4,case 命名 `BraTS-MET-*` 对应 dev list `sub=METS`)
  - 五挑战抽检:shape (256,256,128)、dtype int16、affine 对角 (0.94, 0.94, 1.36)、值域 0..~3900(x1000 约定)全部符合 #249 后写出协议
  - 注:环境恢复时 nnunetv2 安装曾把 triton 动到 PyPI 3.7.1,已回滚 DCU 构建 3.5.1(wheels 缓存 `--no-deps --no-index` 直装),pandas/scikit-image/numpy 锚核实未动
- 仪器输入: `/root/private_data/ctmr/runs/p1/dev_monitor/inputs/`(1040 文件 = 260 obs × 4 模态通道;nnUNet 按 BraTS 四通道约定打包,**推理 260 次** = 每 (case, side) 一次四通道预测,2026-09-03 完成,零错误)
- 测量 CSV: `/root/private_data/ctmr/runs/p1/dev_monitor/measurements_dev.csv`(260 obs + header,零 input_fail/run_fail)
- 监控报告: `/root/private_data/ctmr/runs/p1/dev_monitor/report/dev_monitor_diagnostic.{json,md}`(含观察线旗标状态与逐 case 明细)
- 采样协议: `/root/private_data/ctmr/runs/p1/dev_monitor/cohort.json`(配额 + 选择规则)+ `plan.json`(population=dev,260 observations)

### 逐挑战 ET/WT 读数(仪器 v2;2026-09-03)

| 挑战 | gen n | ET 检出 k/n | 检出率 | 空 pred | ET 缺失 real_only | ET vol median gen/real (ml) | ET rel median (CI90) | WT vol median gen/real (ml) | WT rel median (CI90) |
|---|---:|---:|---:|---:|---:|---|---|---:|---|
| GLI | 50 | 50/50 | 1.0000 | 0/50 | 0 | 64.63 / 16.18 | **2.2424** (-0.799, 276.34) | 697.86 / 81.95 | 7.6644 (1.928, 117.00) |
| MEN | 40 | 40/40 | 1.0000 | 0/40 | 0 | 19.28 / 14.47 | 0.2317 (-0.999, 118.38) | 19.92 / 17.25 | 0.2175 (-0.999, 142.45) |
| METS | 24 | 16/24 | **0.6667** | 4/24 | 7 | 0.02 / 4.85 | -0.9927 (-1.000, 2.517) | 1.60 / 31.85 | -0.8803 (-1.000, 144.75) |
| PED | 10 | 10/10 | 1.0000 | 0/10 | 0 | 8.99 / 4.56 | 1.0514 (-0.869, 6.048) | 323.70 / 32.82 | 9.6895 (-0.222, 21.23) |
| SSA | 6 | 6/6 | 1.0000 | 0/6 | 0 | 494.70 / 21.24 | **38.6386** (5.551, 94.255) | 1123.24 / 167.11 | 6.6617 (3.068, 28.866) |

- WT 添注配对注记:METS 有 1 例 real 侧 WT 体积非正,其相对差按 `RelativeDifference` 契约为 undefined,已排除并计数(`n_undefined=1`),不静默丢弃。

### 观察线判定

- **黄旗**(选择面,非验收判定)。fired 规则:
  1. `METS ET 检出率 0.6667 < 0.9`(主旗,与 T8 疗效对照的靶点一致)
  2. `GLI vol_et_rel 中位 2.2424 > 2.0`(体积高估,刚过线;CI90 上尾极重)
  3. `SSA vol_et_rel 中位 38.6386 > 2.0`(**n=6**,CI90 (5.55, 94.26) 极宽——方向性信号,不宜单独定论)

## 4. 结论与后续动作

链路五步 + spec 覆写全部打通,读数为 T8 重训候选 go/no-go 判读提供「重训前」对照锚。形状判读(选择面):

- **METS 是 ET 生成的主要缺口**:检出 16/24、ET 空 pred 4、real_only 7,ET 体积中位近乎 0——与收编裁决的整改方向(METS 黄旗预期)一致;T8 重训候选在此轴上的改善是 go/no-go 的首要观测点
- **GLI/PED/SSA 呈系统性体积高估**(WT rel 中位 7.7/9.7/6.7):检出 healthy 但尺寸超做——重训后 if 仍高,后续票应考察采样侧引导(cfg、anatomy-size 条件)而非仅重训
- **MEN 全轴健康**(rel 中位 ~0.2)
- **SSA/PED 样本量小**(6/10 例),读数方向性参考;配额由 dev list 各挑战占比钉定,不因读数放宽

后续动作:

- [x] 采样完成(520/520 体,2026-09-01,零错误,五挑战抽检通过)
- [x] real 侧数据恢复(gauss 流式 520/520 文件,2026-09-01;Synapse 直连被地域墙拦截,路径作实)
- [x] plan 落盘 → 冻结仪器推理(v2,260/260)→ 测量 CSV → 监控报告(2026-09-03)
- [x] 基线读数回填本记录 §3(逐挑战 ET/WT + 观察线旗标状态;标注仪器 v2)
- [x] T8 重训候选以同链路(`run_dev_monitor_etwt.sh` 换 `CKPT`+`RUN_ID`+全新 `MONITOR_ROOT`)产出对照读数,go/no-go 判读:**停在 dev,不进 L2**——见 `20260903-序列②T8-dev中间检查点-重训候选go-no-go.md`;其原前置(校准重跑与新 ADR)因判读未进 L2 而未触发
- [ ] 观察线收紧与否(首个重训候选 dev 分布出来后):新诊断种子槽位与既有占用不重叠已登记,阈值收紧须显式新票

## 5. 部署事实勘误(2026-09-01 现场核实,后续作业沿用)

实例在 2026-08-30 聚合后经历过一次重置,恢复不全;与本链路相关的事实:

1. **dev 265 例 native real 体为本体已丢失**: `data/phase/raw/` 是符号链接农场,dev case 链接指向 `l2-instrument-calibration/<hash>/dev_raw`(该主本已随聚合/重置消失,断链);真文件恰为 holdout 530(校准固化物,250 GLI/200 MEN/48 METS/20 PED/12 SSA,逐挑战核实与 dev 零交集)。**holdout 530 真文件属 T5 资产,本链路不得挪用。** 恢复路径:Synapse syn64377310 重下载(PAT 待负责人提供,wizard 与 synapseclient 已就位);恢复前 plan 构建延后(`--sampling-only` 先行采样,real 侧不阻塞 GPU 关键路径)
2. **VAE 幸存副本**: `/root/private_data/ctmr/models/autoencoder_v1.pt`(聚合记录所记 `instruments/v1_models` 现不存在;`instruments/` 下仅 nnunet_results)
3. **仪器结果:主本已丢失(勘误 2026-09-01 晚)**:`instruments/nnunet_results/Dataset501..505` 整体是符号链接农场,全部指向 `l2-instrument/<hash>/results` 主本——该主本随聚合后重置消失,早前「完整可用」仅核对了名字层,系误判。负责人选 B 全量重训(新协议:标准 plans、4×DCU DDP、BF16),见 `deploy/experiments/20260901-仪器主本丢失与重训决策.md`;数据/plans/fold 经 hash 对账与原冻结完全同批
4. **DCU 栈重装清单**(重置后按需): wheels 缓存四件(torch/torchvision/triton/flash_attn,`--no-deps --no-index` 直装)+ PyPI 侧 einops、pytest、synapseclient、nnunetv2==2.8.1、cc3d;numpy 锚 1.26.4 未动
5. **配置三件套**: 以仓库 `configs/` 为准(部署树 `/root/ctmr/configs`);nv-phase-* 旧树已删,`run_token_dilution_d.sh` 的 NV_CONFIGS 旧默认值待后续作业票顺带勘误
6. **SSA results 树曾不干净(2026-09-03 续跑发现,已处置)**:`Dataset502_BraTS2023SSA` 下残留首跑空壳目录 `nnUNetTrainer_250epochs_bf16__nnUNetPlans__3d_fullres`(标准 plans 撞 DDP 全局 batch 断言的残骸,仅 404 字节训练日志、无 checkpoint);配方第二步b 的「恰一个 trainer 目录」防线正确拒绝盲跑。已按「归档改名,不删除」先例挪至 `l2-instrument-v2/aborted-first-run/`,五挑战恢复每 Dataset 恰一目录不变量;正主 v2bs8 目录 checkpoint_final 与审计 `completed_at 19:23:02Z` 分钟级吻合
7. **`nnUNet_extTrainer` 是推理侧同样必需的变量(2026-09-03 续跑发现)**:v2 实况 trainer `nnUNetTrainer_250epochs_bf16` 是包外类(`l2-instrument-v2/trainer/`),训练侧经该变量接入而推理侧脚本未设,`predict_entry_point` 报 "Could not find requested nnunet trainer" 响亮死;配方已加 `NNUNET_EXT_TRAINER`(默认 `l2-instrument-v2/trainer`)随 nnUNet 三变量同组 export(commit `7d886a9`)
8. **nnUNet 四通道打包事实(耗时估算口径)**:inputs 1040 文件按 BraTS 四模态约定(`<case>_0000..0003`)打包,推理次数 = 260(每 (case, side) 一次四通道预测)而非 1040;v2 仪器单卡实测 ~5-6s/case,260 次约 25 分钟;后续 T8 同链路照此估
