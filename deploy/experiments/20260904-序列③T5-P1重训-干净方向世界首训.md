# 20260904 序列③T5——P1 重训 run(干净方向世界首训)

**总体结论**:T7 配方沿袭(clip=True 编码 / token 34 冻结 / 写出 affine,超参逐项不动)+ 唯一改动项 = 训练 embedding 树替换为 T2 RAS 修复重编码树 `embeddings_ras/`,4×DCU 训练 11.1h 早停收尾(e45,best e30);dev FID 候选 **epoch_30,m = 3.7957**,`selection.json` 落盘;配方 diff 核对表落盘(协议改动恰 1、新登记偏差 0、不动 28);种子纪律与零 holdout 接触全程成立。**dev FID 选择仪器读数:干净方向世界未追平 T7(3.7957 vs 3.6690,+3.4%)**——本票产出为干净世界候选与选择读数,空间保真判定面在后续 dev ET/WT 监控与 L2(#316/#317),本记录不预判。 | **记录落盘日**: 2026-09-04

## 1. 目的与关联 issue

issue #315(父 #310 序列③)。P1 全参微调 run(序列③ E1,干净方向世界首训):#310 方向审计实测 T7 训练世界的 replay 臂 embedding ~65% 轴序错乱(编码链 resize 目标误用 NIfTI header 原始轴序 dim,被全卷积 DM 静默吃进训练)。本票以 T7(#254)配方为载体执行干净方向世界重训:超参逐项不动,训练数据消费 T2(#312)RAS 修复全量重编码树。重训定位为**干净底座上的候选产出**——「方向修复对空间保真的净效应」判定面在后续监控与验收链,不在本票的 dev FID 选择仪器。

- 关联 issue:#315(本票)、#310(序列③父票)、#312(T2 数据树,前置)、#313(T3 loader 守卫,前置)、#311(T1 撤销,labels 树不变的前提)、#254(T7 配方载体)、#316/#317(下游,阻塞对象)
- 判定状态关联:variant=diagnostic,选择面;不产生任何验收判定;P1 L2 判定维持 FAIL(T8 后世界)

## 2. 运行配置与 run_id

- **run_id**:`p1-20260904T021136Z`——训练拉起时刻戳(作业脚本 `date -u` 生成日志名 `train_20260904T021136Z.log`,2026-09-04T02:11:36Z;trainer 装配完成时刻 = provenance `written_utc` 02:13:16Z)。按 T6/T8 命名惯例 `p1-<UTC启动戳>` 构造,特此声明构造依据。
- **代码**:部署树 `/root/private_data/ctmr/deploy_t5/`(持久盘)自本地分支 `series3-t5-p1-retrain`(544bab5)rsync;其 `src/` 与 `main@be29070` 逐位同(两分支 commit 仅新增 `deploy/jobs/` 作业与监控脚本,不入训练 import 面)——训练面即 main@be29070 = T2 dim 修复(#319)+ T3 loader 方向/形状双守卫(#320)现役形态。
- **作业**:`deploy/jobs/run_p1_retrain_t5.sh`(沿 T7 shell 先例)。`--dry-run` 先行核对:preflight 逐项绿(底座 sha256 对账一致、VAE md5 一致、config T3 终态钉值与 T7 同钉、network config 对基底 run.json 机读一致、cfg10/30 步、P3 e39 在位观测、embeddings_ras 覆盖 train+replay 各 7404(embedding+sidecar)、T2 manifest 15868 行锚、reference bank / RadImageNet / DCU×4 / rendezvous 29500 空闲)、核对表 gate(协议改动 1/偏差 0)通过后正式拉起。
- **provenance**(`ckpt/train_provenance.json`):lr 2e-6、batch_size 1、n_epochs 100、cache_rate 0、frozen_modality_tokens [34]、amp bf16、world_size 4、torch 2.9.0(DCU)、底座 `diff_unet_3d_rflow-mr-brain_v1.pt`。
- **拓扑**:world_size 4(T7 偏差 A 口径沿袭,#310 Implementation Decisions;对基底 run world_size 7 的偏差已在 T7 登记,本次无新登记偏差)。
- **dev 监控形态**:与 T7 逐字同族——trainer 内嵌周期验证 `--val-every 5`(基底评估网格),16 例×4 模态 dev cohort 全卡分片,cfg=10/30 步;早停规则 ADR-0005 钉值(patience 3 / min_epoch 30 / max=100)经 `<ckpt_dir>/.early_stop` 停机;reference bank 预拷 `reference_reinstr` 产物(bank md5 `07dff979…` 与源逐位一致,缓存命中)——**T5 与 T7/基底 dev FID trend 同仪器同栈同 bank 可比**。
- **时长**:2026-09-04T02:11Z 拉起 → 13:16Z 早停写盘(e45)→ 进程收尾,约 **11.1h**;~14.7 min/epoch(含验证摊销;T7 为 ~19-21 min,本 run 无并发 NFS 重编码作业争用,如实登记环境差异)。
- **种子纪律**:内嵌 dev 验证为 per-(case,modality) sha256 合同种子(`CandidateSampler.seed_of`),零 GLOBAL_SEED 判定链接触;零 challenge_registry 诊断槽位消费;**holdout 530 零接触**(dev list 唯一 population)。

### 配方 diff 核对表(AC1)

`runs/p1_t5/recipe_diff_checklist.json`,schema `p1-retrain-recipe-diff/2`,核对口径:超参基底 = run `p1-20260822T131947Z`,配方载体 = T7 run(`p1_t7`,#254)——**diff 的审计主语是 T5 vs T7**:

- **协议改动恰 1 项**:embedding 数据树 `embeddings_cliptrue` → `embeddings_ras`(T2 #312 RAS 修复全量重编码;施用面:训练臂 7404 + replay 臂 7404 训练消费 + dev cohort spacing sidecar 源同树)。
- **新登记偏差 0 项**;world_size 4 列 unchanged(T7 偏差 A 口径沿袭)。
- **不动 28 项**:超参逐项 = 基底真值(lr 2e-6 / bs 1 / n_epochs 100 / cache 0,preflight 逐键强校验)+ T7 三改动沿袭项(clip=True 编码、token 34 冻结、写出 affine)+ scheduler/loss/augment prob/replay 1:1/cfg10/30 步/网格 5/早停钉值/amp/底座锚/train+network config 双钉值/bank/冻结仪器与判定线/P3 e39 不动等。
- **labels 树:不变**(逐位同 T7)——T1(#311)撤销:复核实证旧树已 RAS,重生成空转;且 P1 image-only 训练不消费 labels。issue 票面「消费 T1 新 labels 树」的前提被 #311 改写,本 run 的 labels 输入与 T7 逐位同,如实声明。

### 与 T7 run 的世界差异声明(AC3 专项)

- **embedding 内容**:T7 世界的 replay 臂 ~65% 轴序错乱(#310 方向审计:cliptrue 树 (32,64,64,4)×2953 / (64,32,64,4)×315 混有轴序错乱工件)在本世界构造性消除——`embeddings_ras` 由修复后编码链(RAS 重排后 spatial_shape 决策 resize 目标)全量重编码,manifest 15868 行对账全绿 + 逐条形状守卫全绿(T2 记录);新树非 canonical 形状(如 (32,64,64,4)×3110)是 MR-RATE 几何多样性的真实反映。
- **BraTS 臂**:内容与 cliptrue 同构(方向免疫是 x/y 对称的数据巧合,#310 历史如实登记;T2 双池抽检直通臂 0.0062 与作业 C 锚逐位一致)。
- **labels 输入**:逐位同 T7(T1 撤销,见上)。
- **训练面代码**:较 T7 新增 T3 双守卫(Orientation RAS 加载层归位 + 启动期形状契约——14808 条启动盘点全过)与 T2 编码 dim 修复;两者对本 run 数据树是冗余防御(树本身已干净),如实登记。
- **环境**:同实例(T2 末期延续)、同 torch-dcu 2.9.0/monai 1.6.0;IO 争用低于 T7 训练期(T2 重编码作业并行期)。

## 3. 关键读数与产物路径

产物(sugon,`/root/private_data/ctmr/runs/p1_t5/`):`ckpt/`(epoch_1..45 逐 epoch + dev_eval/ + train_provenance.json + .early_stop)、`selection.json`、`recipe_diff_checklist.json`、`logs/train_20260904T021136Z.log`。

### dev FID trend(同仪器口径:同 RadImageNet 权重 + 同预处理栈 + 同 bank reference_reinstr;括号为 T7 同点读数)

| epoch | 5 | 10 | 15 | 20 | 25 | 30 | 35 | 40 | 45 |
|---|---|---|---|---|---|---|---|---|---|
| **T5 m** | 3.9504 | 4.1899 | 4.4724 | 4.1484 | 4.0627 | **3.7957** | 4.1344 | 4.2465 | 4.1799 |
| T7 m | 4.5212 | 4.2609 | 4.0253 | 4.0331 | **3.6690** | 3.8296 | 3.8124 | 3.8801 | — |
| 基底 m | 3.8291 | 3.9071 | 3.5889 | 3.4289 | 3.6289 | 3.8317 | 3.5644 | — | — |

- **候选(select 合同:argmin mean dev FID,预记录)**:**epoch 30,m = 3.7957**;`selection.json` 落盘(rule=pre-recorded argmin,recorded 2026-09-04T16:33Z)。
- **候选 checkpoint md5**:`epoch_30.pt` = `b0dbb715abb35de3ba2c54c34abd9539`。
- **早停**:`{"reason": "no new best for 3 evals (best 3.7957)", "epoch": 45}`——e30 best 后 e35/e40/e45 三连无新 best,patience 3/3 满于 e45 边界触发,零 kill 干预。
- **loss 曲线**:全程 0.7121–1.0821(T7 同带 0.76–1.06,bs1 单步噪声主导),无发散/平台异常;首 iter 0.8819、末 epoch 45 收尾 0.9456。
- 启动期 T3 守卫读数:14808 条训练 embedding 形状契约盘点全过(~30s,header 读);loader Orientation RAS 对新树(RAS affine)为 no-op 归位(防绕过链修复的工件,纯防御路径,与 T3 单测钉板一致)。

### 初步判读(选择仪器面,非验收判定)

- **T5 best 3.7957 vs T7 best 3.6690:高 0.127(+3.4%);vs 基底同仪器 best 3.4289:高 0.367(+10.7%)**——dev FID 分布面上,干净方向世界未追平 T7/基底水平。
- trend 形态:前段上行至 e15 峰值(4.47)后连续回落至 e30 best,尾段回升后高位横盘(e35/e40/e45 均高于 best)——「中段 best + 尾段平台」族与 T7/基底同构,但**前段形态与两者相反**(T7/基底自 e5 起回落,T5 先升后降)且 best 出现更晚(e30 vs e25)。一个不立假设的如实陈述:replay 臂占训练步一半,其内容世界更换(轴序修复)实质改变了训练分布,T5 的 epoch 轴动力学与 T7 不同族首段;该差异对空间保真读数(ET/WT 族)的方向性,选择仪器 FID 无法回答。
- **判读边界(如实声明)**:dev FID 是分布级选择仪器;本票换世界的目的在空间保真(方向错乱的直接受害面是 P2 掩码条件与 P3 肿瘤加权 loss 的位置正确性、以及 L2 质心/体积读数),其判定面是下游 dev ET/WT 监控链与 L2 全量族(#316/#317),本记录的 FID 读数**不构成**对「干净世界是否更优」的判定。

## 4. 结论与后续动作

1. T5 完成:候选 `epoch_30.pt`(md5 `b0dbb715…`)经预记录 argmin 规则选出,selection.json / recipe_diff_checklist.json / dev_trend.jsonl / .early_stop 全部落盘 run 树;AC1-AC4 达成(见下节对照)。
2. 干净世界候选就位:下游监控票(#316/#317 阻塞本票产出)以 `p1-20260904T021136Z` epoch_30 为对象跑 T6 同款 dev ET/WT 链,回答「方向修复后空间保真读数走位」;MONITOR_ROOT 须用全新目录(`sampling_provenance.json` 钉本候选指纹,T8 先例:同 root 不可复用于其他候选)。
3. dev FID 未追平 T7 的读数连同 9 点 trend 如实上报;判读归负责人,本记录不预判配方后手。
4. T8 判读(「停在 dev」)的对象是 T7 候选 `p1-20260901T141537Z`;#310 已修订其数据前提(方向污染世界)。T5 候选与 T7 候选的 dev 监控对照(T6 同仪器 v2)是下一轮 go/no-go 的自然形态,配方与种子纪律沿 T6/T8 口径。
5. 产物路径速查:训练日志 `logs/train_20260904T021136Z.log`;配方核对表 `recipe_diff_checklist.json`(协议改动 1/新偏差 0/不动 28);逐点 ledger `ckpt/dev_eval/dev_trend.jsonl`;早停件 `ckpt/.early_stop`;部署树 `/root/private_data/ctmr/deploy_t5/`(持久盘)。

### 执行中登记(如实)

- **部署树布局一错即改**:首次 rsync 把作业脚本置于 `deploy_t5/jobs/`(脚本内 `../..` 解析 PROJECT_ROOT 到上级目录,FATAL 响亮死,零副作用);按 T7 树布局(`deploy_t5/deploy/jobs/`)归位后 dry-run 全绿。
- **监控面 pgrep 自匹配误报**:训练收尾核验时 `pgrep -f "modality_label.train"` 匹配到监控命令自身命令行,误报 "still running";严格模式(`pgrep -af "python3 -m ctmr"`)复核训练进程已全部退出,零进程残留。
- **select 时点**:早停写盘 13:16Z、进程收尾后,select 于 16:33Z 执行(会话侧监控转写间隙);候选文件自 e45 eval 后未被触碰(写入方唯一 = 训练进程,13:16Z 已退出)。
- 服务器与实例:与 T2 末期同实例(4×DCU);部署树持久盘(T7 先例);`deploy_t5` 树与 run 树保留,旧树零触碰。

## 验收对照(#315)

- [x] 配方 diff 核对表落盘(超参逐项 = 基底;diff 恰为数据树替换项)——§2,run 树 `recipe_diff_checklist.json`(协议改动恰 1、新登记偏差 0、不动 28;dry-run + 正式拉起双次生成,内容一致)
- [x] 训练完成,dev_trend + selection.json 落盘——内嵌早停 e45(patience 3/3),9 点 trend,候选 epoch_30 m=3.7957;§3
- [x] 实验记录落盘 `deploy/experiments/`(含与 T7 run 的世界差异声明)——本文件 §2「与 T7 run 的世界差异声明」专项
- [x] 种子纪律(正式判定链)+ 零 holdout 接触——内嵌验证合同种子、零 GLOBAL_SEED 判定链接触、零诊断槽位消费;dev list 唯一 population,holdout 530 任何工件零进入;variant=diagnostic,全程无验收判定产生
