# 序列③ T1 前置复核:labels 方向误诊修订(labels 树 RAS 实证)

**总体结论**:票面(#311)的归因前提——「labels 自产链未做方向重排,89% case 错位」——被三臂复核**推翻**:labels 树内容实测**已是 RAS**(原链逐体素重建 20/20 case dice=1.0000);取证时的「LPS 组 FLIPPED」读数被 native 臂**完整复现**,证实取证协议是 native LPS t1c 直通对位 RAS labels 的**跨世界错位**,不是 labels 工件缺陷;P2/P3 真实训练世界(embedding decode)对位 **AUC 5/5 as-is 最高(0.81–0.97)**——掩码条件与肿瘤加权 loss 实际打在正确位置。全量 labels 重生成是空转,T1 撤销;#310 第 2 条归因改写为仪器链 real 侧跨世界读数(归 #314 修复面)。 | **记录落盘日**: 2026-09-03

## 1. 目的与关联 issue

T1(#311)按票面要求先做实现前取证:还原 labels 生成链、核实「对位探针 LPS/RAS 两组」的取证协议,以便重生成器的单测与退出探针与取证读数可比。复核在 sugon 上实测后发现票面归因不成立,停 T1(负责人裁决:落盘复核 + 修订票面)。

- 关联 issue:#311(本票)、#310(序列③父 spec,第 2 条归因)、#314(T4 仪器链,真实修复面归属)、#247/#255(序列②,误诊读数的世界)
- 判定状态关联:variant=diagnostic 复核,不产生任何验收判定;只读旧树与现役工件,零写入(除 `/tmp` 探针 env)

## 2. 背景:被复核的归因链

Issue #310 第 2 条(2026-09-03 方向审计)断言:labels 全树「硬标 RAS affine」,同 case 对位测试 LPS 组(89%)肿瘤区增益 −67~−289(flipy 后 −8~−44,FLIPPED),RAS 组(+265~+1663)正常——推论 P2 掩码条件、P3 肿瘤加权 loss(129/130/131 权重 100)在 89% 样本打在错误位置,T1 据此立项全量重生成。

## 3. 复核设计(三臂,全部只读)

服务器:sugon 实例(2026-09-03 在役);python 环境同 T4 登记(monai 1.6.0 / nibabel 5.4.2 / torch-dcu 2.9.0);生成链语义自 git 历史 `scripts/brats_phase_prep.py`(退役于 #143,commit `99dff6d^`)还原——`LabelVolumeBuilder`:`LoadImage → EnsureChannelFirst → Orientation(axcodes="RAS") → Resize((256,256,128), nearest)`,brain=22 = 四模态 >0 union,remap 1→129/2→130/3→131。

| 臂 | 输入 | 读数 | 回答的问题 |
|---|---|---|---|
| ① 重建一致性 | nnunet seg + 4 模态(raw_relinked 真身)按原链重建 | 与树上 combined/tumor129 的 tumor/brain dice | 树上工件是否已是忠实 RAS 产物 |
| ② native 直通对位 | **native(未 RAS 化)t1c** resize 到 (256,256,128) vs 树上 labels | 肿瘤-亮度增益(ET 区均值 − brain 区均值),as-is/flipy/flipxy | 取证读数能否被「native 协议」复现 |
| ③ decode 世界对位 | clip=True embedding → 冻结 VAE decode(autoencoder_v1,链同 VaeReconstructor)vs 树上 labels | Mann-Whitney AUC(P(recon[ET]>recon[brain])),as-is/flipy/flipxy | **P2/P3 真实训练条件世界**里 label 与 image 是否对位 |

## 4. 关键读数

### 4.1 输入方向基数(全量)

- nnunet labelsTr(train 侧 1852 seg)axcodes:**LPS 1656 / RAS 196 = 89.4% / 10.6%**——与 #310 记载的官方源比例一致,上游方向混合属实
- labels 树 5292 文件 = (train 1852 + dev 264)cases × 2(combined/tumor129)− 1 排除 case × 2 + holdout 530 × 2 = 4232 + 1060

### 4.2 臂①:labels 树 = 原链忠实 RAS 产物(20/20)

5 挑战 × 各 train 前 4 case(GLI/MEN/PED 全 LPS,METS/SSA 全 RAS),tumor 区(≥129)与 brain 区(=22)dice **全部 = 1.0000**(brain 最低 0.9979,MEN-00027);逐轴翻转对照(dice 0.0001–0.61)排除巧合。affine(origin x=−239、zoom 1)是 Orientation RAS 翻转 x 轴的正确产物,非「硬标」。

### 4.3 臂②:native 直通对位完整复现取证读数

| case | 方向 | as-is | flipy | flipxy |
|---|---|---:|---:|---:|
| BraTS-GLI-00154-000 | LPS | **−106.4** | −14.4 | +238.9 |
| BraTS-MEN-00232-000 | LPS | **−133.5** | +22.9 | +68.5 |
| BraTS-MET-00242-000 | RAS | **+202.5** | +173.9 | +223.9 |

与 #310「LPS 组 base −67~−289 / flipy −8~−44(FLIPPED);RAS 组 +265~+1663(OK)」**同模式**:LPS case 崩塌、单 flipy 部分回升、flipxy(RAS→LPS 等价)恢复——取证协议即 native t1c 直通,未先 RAS 化。

### 4.4 臂③:P2/P3 训练世界对位(决定性)

embedding(编码链 Orientation RAS 产物)→ VAE decode 重建 (256,256,128) 影像,与树上 labels 对位 AUC:

| case | 方向 | as-is | flipy | flipxy |
|---|---|---:|---:|---:|
| BraTS-GLI-00154-000 | LPS | **0.926** | 0.127 | 0.137 |
| BraTS-MEN-00232-000 | LPS | **0.973** | 0.274 | 0.259 |
| BraTS-GLI-01212-000 | LPS | **0.810** | 0.686 | 0.572 |
| BraTS-MET-00242-000 | RAS | **0.944** | 0.558 | 0.645 |
| BraTS-SSA-00215-000 | RAS | **0.853** | 0.128 | 0.119 |

**5/5 case as-is 最高、flip 臂全崩(含 LPS 组)**——训练世界(image=embedding RAS,label=labels RAS)同世界对位。P2 掩码条件、P3 肿瘤加权 loss 未打错位置。

### 4.5 执行面登记:dev 侧 seg 数据链断

- `phase/raw/` 全树 2647 个 `*-seg.nii.gz` 均为符号链接,指向已消失的 `/root/private_data/brats2023_nnunet/`(重组前布局)——全部断链
- seg 现役真身仅 `data/nnunet_raw/Dataset50X/labelsTr/`(train 侧 1852);**dev 侧 264 case 的 seg 当前不可得**;`raw_brats2023_official`(dev 侧官方布局)只含 4 模态、无 seg
- 后果:任何以官方 seg 为输入的全量作业(含原票面重生成)在补链前只能覆盖 train 侧

## 5. 结论与后续动作

- [x] 三臂复核完成,票面归因推翻(本记录)
- [x] T1 撤销全量重生成(负责人裁决 2026-09-03);#311 票面改写、#310 第 2 条归因修订(见 issue 评论)
- [x] 旧 labels 树零改动(复核全程只读)

后续动作:

- **真实修复面改判**:11% RAS case gen/real 错位的最简一致解释移至仪器链——real 侧直通 native(方向混合)+ `DM_GRID_TO_LPS_AXIS_FLIP` 无条件 flip,对 LPS real 恰好对齐、对 RAS real 错位(与 #310 第 4 条「11% RAS case x/y 错位全幅」吻合);修复归属 #314(affine 驱动 RAS 统一重采样 + flip 退役为同世界断言)
- **对位探针语义修订**:「LPS/RAS 两组全 OK」作为退出条件在 native 直通协议下对任何 labels 工件都不可达(t1c raw 本身 native);探针须同世界读法(t1c 先 affine 驱动 RAS 化,或走 decode 世界),建议并入 #314 的两侧同世界断言
- T2(编码 dim 决策,replay 形状错乱有形状分布证据)、T3 锚 clip 对齐不受本复核影响,照常推进;T3 的「LPS 标签工件加载后 RAS」守卫降级为纯防御(labels 实测已 RAS),无害保留
- 若未来任何作业需以官方 seg 为输入覆盖 dev 侧:先补 dev 侧 264 case 的 seg 数据链(§4.5),并登记新真身布局
- 序列② T5/T6 读数与 T8 判读中引用「labels 错位 89%」的段落,由 #314 的 T8 判读修订一并改写(其判定结论本身——T7 候选停在 dev——不受影响,归因链修订)
